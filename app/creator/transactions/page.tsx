import { CreatorTransactionsClient } from './client'

export const metadata = {
  title: "Sales — Creator Dashboard",
}

export default function CreatorTransactionsPage() {
  return <CreatorTransactionsClient />
}